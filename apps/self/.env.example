# Server Configuration
PORT=3000
CORS_ORIGIN=*

# Authentication
# Generate a secure key for production: openssl rand -hex 32
BOOTSTRAP_API_KEY=your-secure-bootstrap-key-here

# SQLite (KV storage)
# Path to SQLite database file
SQLITE_PATH=./data/kv.db

# S3/MinIO (Blob storage)
# For local MinIO: http://localhost:9000
# For AWS S3: https://s3.amazonaws.com
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_REGION=us-east-1
S3_PRIVATE_BUCKET=federise-private
S3_PUBLIC_BUCKET=federise-public

# Optional: Public URL for blob downloads
# If using MinIO with public bucket, this would be the MinIO endpoint
PUBLIC_DOMAIN=http://localhost:9000/federise-public
