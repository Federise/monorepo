# Server Configuration
PORT=3000
CORS_ORIGIN=*

# Authentication
# This key is used for initial setup to create principals
BOOTSTRAP_API_KEY=your-bootstrap-key-here

# Deno KV (optional)
# If not set, uses Deno's default location
# For compiled binary, this will be relative to where the binary runs
# KV_PATH=./data/kv.db

# S3-Compatible Storage (MinIO, AWS S3, etc.)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_REGION=us-east-1
S3_PRIVATE_BUCKET=federise-private
S3_PUBLIC_BUCKET=federise-public

# Optional: Public URL for blob downloads (if using CDN or different domain)
# PUBLIC_DOMAIN=https://cdn.example.com
