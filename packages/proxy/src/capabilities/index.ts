/**
 * Capability store implementations for the proxy package.
 */

export { CookieCapabilityStore } from './cookie-store';
export type { CookieCapabilityStoreOptions } from './cookie-store';

// Re-export types for convenience
export type { CapabilityStore, PermissionRecord, Capability } from '../types';
